---
import BaseLayout from './BaseLayout.astro';
import Footer from '../components/Footer.astro';
import { siteConfig } from '../data/site';
import { withBase } from '../utils/paths';

interface Props {
  title: string;
  description?: string;
  eyebrow?: string;
  heroImage?: string;
  pageTitle?: string;
}

const { title, description, eyebrow, heroImage, pageTitle } = Astro.props;
const headTitle = pageTitle ?? `${title} — ${siteConfig.name}`;
---

<BaseLayout title={headTitle} description={description} image={heroImage}>
  <div class="page">
    <header class="page-nav">
      <a class="page-nav__brand" href={withBase('/')}>{siteConfig.shortName} <span>Portfolio</span></a>
      <nav aria-label="Primary">
        {
          siteConfig.navItems.map((item) => (
            <a
              href={item.href}
              target={item.external ? '_blank' : undefined}
              rel={item.external ? 'noreferrer' : undefined}
            >
              {item.label}
            </a>
          ))
        }
      </nav>
    </header>

    <main class="page-main">
      {
        heroImage && (
          <div class="page-hero">
            <img src={heroImage} alt="" />
          </div>
        )
      }

      <section class="page-head">
        {eyebrow && <div class="page-eyebrow">{eyebrow}</div>}
        <h1 class="page-title">{title}</h1>
        {description && <p class="page-deck">{description}</p>}
      </section>

      <div class="page-body">
        <slot />
      </div>
    </main>

    <Footer />
  </div>
</BaseLayout>
